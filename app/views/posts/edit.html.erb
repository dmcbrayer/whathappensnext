<h1>Edit Post</h1>

<%= form_for @post, url: story_post_path(@story, @post) do |f| %>
  <div class="form-group">
    <%= f.text_area :body, class: "form-control", :rows => 25 %>
  </div>

  <%= f.submit "Publish", class: "btn btn-primary" %>
<% end %> 
